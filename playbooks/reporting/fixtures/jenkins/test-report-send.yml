---
# --- Start of playbook test_report_send.yml variables ---
# --- Automatically generated on 2025-06-09 at 13:37:46 +0300IDT ---
# --- Generating user 'boofoo' ---
# splunk_url: https://httpstat.us/403
splunk_url: https://httpbingo.org/status/403
splunk_token: 0f5f7d4c-8d68-41ac-8374-79592a78d5a3 # not real
splunk_channel: 25027c3b-6ec5-4b22-bc71-0a5bcd94da1e # not real
splunk_index: myindex-devel
ci_service_url: "{{ lookup('env', 'JENKINS_URL') }}"
rmg_ci_system: jenkins
output_dir: "{{ playbook_dir}}/output"

####################################################################
## ROLE: junit2json
####################################################################
junit2_do_merge: true
junit2_input_reports_list:
  - "{{ playbook_dir }}/fixtures/jenkins/test_e2e_performanceprofile_functests_3_performance_status__report.xml"
  - "{{ playbook_dir }}/fixtures/jenkins/test_e2e_performanceprofile_functests_9_reboot__report.xml"
  - "{{ playbook_dir }}/fixtures/jenkins/test_e2e_performanceprofile_functests_0_config__report.xml"

junit2_output_merged_report: "merged.junit.json"

####################################################################
# ROLE: report_metadata_gen
####################################################################
rmg_metadata_output_path: "{{ output_dir }}/metadata.json"
# report_combine role:
rc_collector_format: splunk
rc_metadata_path: "{{ rmg_metadata_output_path }}"
rc_report_path: "{{ output_dir }}/{{ junit2_output_merged_report }}"
rc_combined_event_path: "{{ output_dir }}/combined-event.json"

####################################################################
# ROLE: report_send
####################################################################
rs_collector_url: "{{ splunk_url }}"
rs_collector_auth_token: "{{ splunk_token }}"
rs_collector_target: "{{ splunk_channel }}"
rs_collector_source: "{{ ci_service_url }}"
rs_combined_event_path: "{{ rc_combined_event_path }}"

####################################################################
# Directory for any output generated by the junit2json processing
####################################################################
junit2_output_dir: "{{ output_dir }}"

####################################################################
# debug variables:
####################################################################
rs_event_save: true
rs_do_send: false
rs_event_dump_file: "{{ output_dir }}/actual-event.json"
# --- End of variables for playbook test_report_send.yml ---
