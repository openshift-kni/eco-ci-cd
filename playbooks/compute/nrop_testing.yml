- name: Nrop Deploy testing
  hosts: bastion
  gather_facts: false
  environment:
    K8S_AUTH_KUBECONFIG: "{{ kubeconfig }}"

  tasks:
    - name: Run role nrop_testing
      ansible.builtin.include_role:
        name: nrop_testing
